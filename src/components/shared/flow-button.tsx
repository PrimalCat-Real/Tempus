import React from 'react'
import { Button } from '../ui/button'

const FlowButton = () => {
    return (
        <Button className="primary-button-gradient text-secondary-foreground">
            Download
        </Button>
    )
}

export default FlowButton
